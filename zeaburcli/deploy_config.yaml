# Zeabur 部署配置文件 (gcli2api)
# 正式服务器配置从 MySQL servers 表读取
# 此文件仅保留全局配置和测试服务器

# API Token (也可通过环境变量 ZEABUR_API_TOKEN 设置)
api_token: "sk-27orbwrp6wszn4x57ogug3c556uvw"

# MySQL 连接 (也可通过环境变量 MYSQL_URI 设置)
# 部署脚本从 servers 表读取 zeabur_service_id 和 zeabur_environment_id
mysql_uri: "mysql://root:Ljy13145920!@usw-cynosdbmysql-grp-qktg6vkd.sql.tencentcdb.com:21102/gmailmanagement"

# 并行部署数量
parallel_count: 5

# 打包排除规则 (相对于项目根目录)
exclude_patterns:
  - ".git"
  - ".git/**"
  - ".github"
  - ".github/**"
  - ".venv"
  - ".venv/**"
  - "__pycache__"
  - "creds/**"
  - ".env"
  - ".env.example"
  - "*.log"
  - "log.txt"
  - "scripts/**"
  - "zeaburcli/deploy_config.yaml"
  - "zeaburcli/**"

# 测试服务器 (仅用于本地测试，正式服务器从数据库读取)
services:
  - name: "g105"
    service_id: "69591f06b8dd347fac238730"
    environment_id: "69591ee3a7aaff0c115281e8"
